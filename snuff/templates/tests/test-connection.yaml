apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "snuff.fullname" . }}-test-connection"
  labels:
    {{- include "snuff.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "snuff.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
