apiVersion: v1
kind: Secret
metadata:
  name: {{ include "snuff.fullname" . }}-secret
type: Opaque
stringData:
{{- range $key, $value := .Values.env.secret }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
